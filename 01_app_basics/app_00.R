library(shiny)

ui <- fluidPage(
  
)  #ui element, handles controls and displaying plots

server <- function(input, output) {
  
} #server element, handles calculations

shinyApp(ui = ui, server = server)
